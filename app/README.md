# App Bootstrap Note

이 디렉터리는 Flutter 도메인 코드와 앱 구조를 먼저 잡아둔 상태입니다.

현재 워크스페이스에서 `flutter create`가 타임아웃되어 Android/iOS 네이티브 셸은 아직 자동 생성하지 못했습니다.
로컬 환경이 안정되면 아래 명령으로 이어서 생성하면 됩니다.

```bash
flutter create --platforms=android,ios .
```

